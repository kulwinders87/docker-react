sudo: required
services:
  - docker

before_install:
  - docker build -t kulwinders/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true kulwinders/docker-react npm run test -- --coverage